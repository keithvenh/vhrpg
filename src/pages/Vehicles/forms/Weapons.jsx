export default function Weapons({vehicle, handler}) {
  return (
    <div className='Weapons'>
      <h1>{vehicle.name || vehicle.displayName || "Vehicle"} Weapons</h1>
      <h2>We're sorry, but this page is under construction.</h2>
    </div>
  )
}