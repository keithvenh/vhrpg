export default function Equipment({vehicle, handler}) {
  return (
    <div className='Equipment'>
      <h1>{vehicle.name || vehicle.displayName || "Vehicle"} Equipment</h1>
      <h2>We're Sorry, but this page is under construction.</h2>
    </div>
  )
}